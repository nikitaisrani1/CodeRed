<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.righttickk.CodeRed">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CodeRed"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CodeRed">
        <activity android:name=".LogDataActivity"></activity>
        <activity android:name=".LogListActivity" />
        <activity android:name=".BmiActivity" />
        <activity android:name=".HaveAccountActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".ManageActivity" />
        <activity android:name=".ThirdIntroActivity" />
        <activity android:name=".SecondIntroActivity" />
        <activity
            android:name=".FirstIntroActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>